{
  "name": "html-lablink-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=9002 --open=index.html",
    "dev": "live-server --port=9002 --open=index.html"
  },
  "dependencies": {
    "date-fns": "^3.6.0",
    "lucide-static": "^0.395.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
