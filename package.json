{
  "name": "html-lablink-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx live-server --port=9002 --open=index.html",
    "dev": "npx live-server --port=9002 --open=index.html"
  },
  "dependencies": {
    "lucide-static": "^0.395.0"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
