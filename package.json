
{
  "name": "olms-main",
  "version": "0.1.0",
  "private": true,
  "description": "Optimized Lab Management System - Frontend",
  "scripts": {
    "start": "cd backend && npm run dev",
    "dev": "cd backend && npm run dev",
    "frontend-only": "npx live-server --port=9002 --open=index.html"
  },
  "dependencies": {
    "lucide-static": "^0.395.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
